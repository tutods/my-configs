# Introduction

```js
// test js
console.log("hello, world");
```

```tsx
import { ReactNode } from "react";

type Props = {
  className?: string;
  children: ReactNode;
};

const Comp = ({ className = "", children }: Props) => {
  return <div className={`${className}`}>{children}</div>;
};
```

<div className="flex items-center gap-4 text-center text-red-500"></div>
